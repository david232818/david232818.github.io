			[TeamH4C] 코드업 1085번


						작성자: 정권영



---[ Contents
     1. 1085번 문제
     2. 문제 풀이
     3. References





---[ 1. 1085번 문제
 [1]은 다음과 같이 문제를 제시한다.

-------------------------------------------------------
1초 동안 마이크로 소리강약을 체크하는 수를 h
(헤르쯔, Hz 는 1초에 몇 번? 체크하는가를 의미한다.)

한 번 체크한 결과를 저장하는 비트 b
(2비트를 사용하면 0 또는 1 두 가지, 16비트를 사용하면 65536가지..)

좌우 등 소리를 저장할 트랙 개수인 채널 c
(모노는 1개, 스테레오는 2개의 트랙으로 저장함을 의미한다.)

녹음할 시간 s가 주어질 때,

필요한 저장 용량을 계산하는 프로그램을 작성해보자.

실제로 일반적인 CD 음질(44.1KHz, 16bit, 스테레오)로 1초 동안 저장하려면
44100 * 16 * 2 * 1 bit의 저장공간이 필요하다.

**
      8 bit(비트)           = 1byte(바이트)     //       8bit=1Byte
1024 Byte(210 byte) = 1KB(킬로 바이트) // 1024bit=1KB
1024 KB(210 KB)      = 1MB(메가 바이트)
1024 MB(210 MB)     = 1GB(기가 바이트)
1024 GB(210 GB)      = 1TB(테라 바이트)

입력
h, b, c, s 가 공백을 두고 입력된다.
h는 48,000이하, b는 32이하(단, 8의배수), c는 5이하, s는 6,000이하의 자연수이다.

 

출력
필요한 저장 공간을 MB 단위로 바꾸어 출력한다.
단, 소수점 둘째 자리에서 반올림해 첫째 자리까지 출력하고 MB를 공백을 두고 출력한다.
-------------------------------------------------------

---[ 2. 문제 풀이
 [2, p. 15]는 EXAMPLE5에서 floating-point expression이 정밀도의 한계로
인해 재배열에 제한이 있어 implementation이 덧셈 또는 곱셈에 대한 수학적
결합 법칙이나 분배 법칙을 일반적으로 적용할 수 없고 이는 roundoff error에
의한 것으로 오버플로우나 언더플로우가 없더라도 같다고 설명한다. 이러한 이유로
float 또는 double 타입의 연산을 사용할 때는 괄호를 쓰는 것에 대해 주의할
필요가 있고 수학적 연산 법칙의 기능을 염두에 둔 괄호는 가능한 쓰지 않는 것이
좋다.

 상기 서술한 floating-point expression에서 발생하는 문제를 생각하면 다음과
같은 코드를 작성할 수 있다.

-------------------------------------------------------
#include <stdio.h>

int main()
{
    int h, b, c, s;
    double res;
    
    scanf("%d %d %d %d", &h, &b, &c, &s);
    res = (double) h * b * c * s / 8388608;
    printf("%.1f MB\n", res);
    return 0;
}
-------------------------------------------------------

---[ 3. References
[1] "[기초-종합] 소리 파일 저장용량 계산하기(설명)", CodeUP, 2012.
[Online]. Available: https://codeup.kr/problem.php?id=1085,
[Accessed May. 08, 2022]

[2] ISO/IEC JTC 1/SC 22/WG 14, "ISO/IEC 9899:1999, Programming
languages -- C", ISO/IEC, 1999

